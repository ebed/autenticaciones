<p id="notice"><%= notice %></p>


<h1 class="page-header">Pacientes</h1>

<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Nombre</th>

        <th>Apellido</th>
        <th>Fecha Nacimiento</th>
        <th>Dirección</th>
        <th colspan="3">
          <%= link_to new_patient_path, class: "btn btn-success btn-xs" do %>
           <i class="glyphicon glyphicon-plus"> </i>
          <% end %>

        </th>
      </tr>
    </thead>
    <tbody>

    <% @patients.each do |patient| %>
      <tr>
        <td><%= patient.persona.first_name %> <%= patient.persona.second_name %></td>
        <td><%= patient.persona.last_name %></td>
        <td><%= patient.persona.dob %></td>
        <td>

        </td>
        <td>

        <div class="btn-group  btn-group-sm" role="group" aria-label="...">
          <%= link_to patient, class: "btn btn-info" do %>
            <i class="glyphicon glyphicon-eye-open"></i>
          <% end %>

          <%= link_to  edit_patient_path(patient), class: "btn btn-primary" do %>
            <i class="glyphicon glyphicon-edit"></i>
          <% end %>
          <%= link_to  patient, method: :delete, data: { confirm: 'Seguro?' }, class: "btn btn-danger"  do %>
            <i class="glyphicon glyphicon-remove" ></i>
          <% end %>
        </div>


          </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

