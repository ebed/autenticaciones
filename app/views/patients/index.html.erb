<p id="notice"><%= notice %></p>


<h1 class="page-header">Pacientes</h1>

<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Nombre</th>

        <th>Apellido</th>
        <th>Fecha Nacimiento</th>
        <th>Dirección</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>

    <% @patients.each do |patient| %>
      <tr>
        <td><%= patient.persona.first_name %> <%= patient.persona.second_name %></td>
        <td><%= patient.persona.last_name %></td>
        <td><%= patient.persona.dob %></td>
        <td>

        </td>
        <td><%= link_to 'Detalle', patient %></td>
        <td><%= link_to 'Editar', edit_patient_path(patient) %></td>
        <td><%= link_to 'Eliminar', patient, method: :delete, data: { confirm: 'Seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Paciente', new_patient_path %>
