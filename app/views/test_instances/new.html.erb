<h1>New Test Instance</h1>

<%= render 'form' %>

<%= link_to 'Back', patient_ficha_registro_path(:ficha_id => params[:ficha_id], :id => @test_instance.id, :patient_id => params[:patient_id], :id => params[:registro_id]) %>
