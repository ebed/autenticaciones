<h1 class="page-header">Pruebas</h1>

<div class="table-responsive">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Nombre</th>

        <th>Detalle</th>

        <th colspan="3">
          <%= link_to new_test_path, class: "btn btn-success btn-xs" do %>
           <i class="glyphicon glyphicon-plus"> </i>
          <% end %>

        </th>
      </tr>
    </thead>
    <tbody>

    <% @tests.each do |test| %>
      <tr>
        <td><%= test.nombre %>  </td>
        <td><%= test.detalle %></td>
        <td>

        <div class="btn-group  btn-group-sm" role="group" aria-label="...">
          <%= link_to test, class: "btn btn-info" do %>
            <i class="glyphicon glyphicon-eye-open"></i>
          <% end %>

          <%= link_to  edit_test_path(test), class: "btn btn-primary" do %>
            <i class="glyphicon glyphicon-edit"></i>
          <% end %>
          <%= link_to  test, method: :delete, data: { confirm: 'Seguro?' }, class: "btn btn-danger"  do %>
            <i class="glyphicon glyphicon-remove" ></i>
          <% end %>
        </div>


          </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
